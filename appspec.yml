version: 0.0

os: linux

files:
  - source: /
    destination: /home/ec2-user/misskey-deployment

permissions:
  - object: scripts
    pattern: "**"

hooks:
  AfterInstall: 
    - location: scripts/after_install.sh
      timeout: 180

  ApplicationStart: 
    - location: scripts/application_start.sh
      timeout: 180